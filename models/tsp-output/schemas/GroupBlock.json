{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "GroupBlock.json",
    "type": "object",
    "properties": {
        "kind": {
            "type": "string",
            "const": "group"
        },
        "children": {
            "type": "array",
            "items": {
                "$ref": "ContentBlock.json"
            },
            "minItems": 1,
            "description": "The child content blocks within this group."
        },
        "layout": {
            "type": "string",
            "description": "Layout hint controlling how children are arranged."
        }
    },
    "required": [
        "kind",
        "children"
    ],
    "description": "A generic container for nested content blocks.\n\nReplaces the previous `fragment` and `columns` block types with a single\nunified container. The `layout` hint controls arrangement:\n\n- `\"stack\"` (default) — vertical stacking, progressive reveal\n- `\"columns\"` — side-by-side column arrangement\n- `\"rows\"` — horizontal row arrangement\n\nEngines MAY support additional layout hints via the `x-` extension prefix."
}