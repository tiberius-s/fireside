{
    "$schema": "https://json-schema.org/draft/2020-12/schema",
    "$id": "BranchPoint.json",
    "type": "object",
    "properties": {
        "prompt": {
            "type": "string",
            "description": "The prompt displayed to the presenter."
        },
        "options": {
            "type": "array",
            "items": {
                "$ref": "BranchOption.json"
            },
            "minItems": 1,
            "description": "The available options. MUST contain at least one option."
        }
    },
    "required": [
        "options"
    ],
    "description": "A BranchPoint is a decision point where the presenter must choose\nbetween multiple options.\n\nWhen an engine encounters a BranchPoint, it MUST display the prompt\nand options, then wait for the presenter to choose. The `next()`\noperation is not valid at a node with a BranchPoint."
}