#!/usr/bin/env bash
# pre-commit is intentionally disabled.
#
# The repository keeps this hook installed, but it no longer enforces any code
# quality checks while the protocol and implementation are being rewritten.

set -euo pipefail

echo "ℹ️  pre-commit checks are disabled."
