#!/usr/bin/env bash
# pre-push is intentionally disabled.
#
# The repository keeps this hook installed, but it no longer enforces lint,
# docs, commit-level checks, or tests while the protocol and implementation are
# being rewritten.

set -euo pipefail

echo "ℹ️  pre-push checks are disabled."
